const DeploymentActionType = Object.freeze({
    CREATE: "CREATE",
    START: "START",
    RESTART: "RESTART",
    REMOVE: "REMOVE",
    UNCHANGED: "UNCHANGED"
});

export default DeploymentActionType;